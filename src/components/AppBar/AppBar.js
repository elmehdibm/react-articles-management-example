import React from 'react'
import PropTypes from 'prop-types'

const AppBar = props => {
    return (
        <div>
            App Bar
        </div>
    )
}

AppBar.propTypes = {

};

export default AppBar;
